#include <iostream>
using namespace std;
int main(){
    bool x = true, y = false;
    cout<<"x && y : "<<(x&&y)<<endl; // Logical AND operator
    cout<<"x || y : "<<(x||y)<<endl; // Logiacl OR operator
    cout<<"!x : "<<(!x)<<endl; // Logiacl NOT operator
    return 0;
}